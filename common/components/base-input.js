import styled from "styled-components"

const BaseInput = styled.input`
  margin: auto;
  border-radius: 1em;
  text-align: center;
  display: block;
  font-size: inherit;
`

export default BaseInput
