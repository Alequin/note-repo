
const postgresCommand = require("database/postgres-command")
const {notesSchema} = require("database/schema")

const INSERT_NOTE = `INSERT INTO`

function insertNote(){

}

module.exports = insertNote
